# Path to a file containing a 64-hex private key for Somnia Shannon testnet (STT gas + tUSDC).
# The key is only ever used to place/cancel/redeem on the DreamDEX EC venue; caps are in live.ts.
PIN_KEY_FILE=data/pin_key.txt
# 1 (default) = shadow quotes only, 0 = real orders
# LIVE_DRY=1
